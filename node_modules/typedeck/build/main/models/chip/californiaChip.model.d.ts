import { ChipColorType } from './chipColorType.model';
import { ChipColor } from './chipColor.model';
import { Chip } from './chip.model';
/**
 * Represents a California valuing Chip
 */
export declare class CaliforniaChip extends Chip {
    constructor(color: ChipColor, colorType?: ChipColorType, overrideValue?: number);
    private configureDefaultColorValues;
}
