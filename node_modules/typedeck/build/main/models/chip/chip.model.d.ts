import { IChip } from './chip.interface';
import { ChipColor } from './chipColor.model';
import { ChipColorType } from './chipColorType.model';
/**
 * Represents a generic chip - typically used
 * in Poker style games.
 */
export declare class Chip implements IChip {
    color: ChipColor;
    colorType: ChipColorType;
    overrideValue: number;
    valueMap: Map<ChipColor, number>;
    constructor(color: ChipColor, colorType?: ChipColorType, overrideValue?: number);
    toString(): string;
    getValue(_color?: ChipColor): number;
    setValue(amount: number): this;
    setColorValue(color: ChipColor, amount: number): this;
    getIndex(): string;
}
