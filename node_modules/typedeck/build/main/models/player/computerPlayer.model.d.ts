import { Player } from './player.model';
import { IHand } from '../cardCollection/hand.interface';
import { Difficulty } from './difficulty.model';
export declare class ComputerPlayer extends Player {
    difficulty: Difficulty;
    constructor(name?: string, hand?: IHand, difficulty?: Difficulty);
    static Create(fields?: ComputerPlayer): ComputerPlayer;
    setDifficulty(difficulty: Difficulty): this;
    toString(): string;
}
