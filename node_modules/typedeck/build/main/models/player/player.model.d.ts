import { IRandomStringService } from '../../common/randomStringService.interface';
import { IHand } from '../cardCollection/hand.interface';
import { IPlayer } from './player.interface';
/**
 * Represents a Player
 */
export declare class Player implements IPlayer {
    name: string;
    private hand;
    id: string;
    score: number;
    constructor(name?: string, hand?: IHand, randomStringService?: IRandomStringService);
    updateScore(score: number): this;
    getHand(): IHand;
    setHand(hand: IHand): this;
    toString(): string;
    getIndex(): string;
}
